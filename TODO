
- Document use_germline_db(), list_germline_dbs().

- igblastn() should return the results in a data.frame (or tibble?)
  when 'outfmt' is set to "AIRR" (the default).

- Add unit tests.

- Implement install_AIRR_germline_db(). Will download the germline sequences
  from https://ogrdb.airr-community.org/ (link provided by Kellie).

- Implement install_NCBI_germline_db().

- Update README.md.

- Duplicate seq_ids errors: happens when running make_blast_dbs() on FASTA
  file with duplicate seq_ids. For example:
  - on C-region db IMGT.human:
      BLAST Database creation error: Error: Duplicate seq_ids are found:
      LCL|IGHA1*01
  - on germline db IMGT-202430-2.Homo_sapiens.TR:
      BLAST Database creation error: Error: Duplicate seq_ids are found:
      LCL|TRAV14/DV4*01

